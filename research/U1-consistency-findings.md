# Consistency findings from the glossary work (U1)

While checking every glossary entry against the engine and skills, the writing and verification agents found places where the skills, the questionnaire, the spec and the engine disagree. Fixed items are marked. All of them were fixed by 2026-09-24 (items 4-15 by the F1 lane).

| # | Where | Mismatch | Status |
|---|---|---|---|
| 1 | `skills/opendesigner/references/rules.md` section 9 | Example said `roundness=45` is "a bit softer"; the engine maps 45 to 6px, sharper than the 8px default (65 gives 12px). | **Fixed** 2026-09-24: example now uses 65. Rebuilt `chatgpt-project/` and synced skill copies. |
| 2 | `tools/build_data.py` (tool-hook table parser) | The five tool rows in `hooks.json` had every field shifted one column (the Hook cell holds only the id, so the first split cell was empty). | **Fixed** 2026-09-24: parser drops the empty cell; tool hooks now also list their questions. Engine tests pass (25). |
| 3 | `skills/opendesigner/SKILL.md`, `references/zoom.md` | Told the model to run `engine.py set zoom.all 1`, which the engine rejected. | **Fixed** by R2 (engine now accepts `zoom.all`). |
| 4 | `engine.py` (around lines 3837 and 4121) | Principles are read from Q-brand-04; the principles question is Q-brand-07. | **Fixed** 2026-09-24 (F1): answering Q-brand-07 with a list (or `{principles: [...]}`) sets `principles`; DESIGN.md and PRODUCT.md now point to Q-brand-07. Test: `test_principles_come_from_q_brand_07`. |
| 5 | `engine.py` and Q-tool-03 | Answering Q-tool-03 never sets `exports.figmaPlan`. | **Fixed** 2026-09-24 (F1): Q-tool-03's Figma answers set `exports.figmaPlan` (starter, professional, organization, enterprise); Paper, Penpot and none leave it alone. Test: `test_q_tool_03_sets_the_figma_plan`. |
| 6 | Q-dir-01 and the engine | Q-dir-01's default is `flat2`, but if it is never answered no preset applies. | **Fixed** 2026-09-24 (F1): until Q-dir-01 is answered, its default preset `flat2` applies (`effective_preset`), and DESIGN.md says so. Answering Q-dir-01 with a value that is not a preset turns presets off. Test: `test_q_dir_01_default_applies_until_answered`. |
| 7 | Stages 23, 26, 27 | Offer defaults for features that do not exist yet (Q-pattern-05, Q-dist-01 to 03, Q-pref-01 and 02). Mark them planned or hide them. | **Fixed** 2026-09-24 (F1): the six questions carry `Status: planned` in QUESTIONNAIRE.md. `build_questionnaire.py` writes `status` and `meta.planned`; `build_data.py` marks them in the stage files, leaves them out of `pacing.json` levels, and the interview protocol and zoom.md say to skip them and record nothing. |
| 8 | `skills/opendesigner-export/SKILL.md` | Describes 2 Figma collections; the engine writes 5. | **Fixed** 2026-09-24 (F1): the export skill lists the five collections the engine writes (Primitives, Color, Density, Motion, Tokens). |
| 9 | `skills/opendesigner-extend/SKILL.md` | Says `review` finds hard-coded shadows and durations; it does not. | **Fixed** 2026-09-24 (F1): `review` now finds raw shadows (CSS `box-shadow`, `text-shadow`, `drop-shadow()`, Swift and Compose `.shadow`) and durations (CSS transitions and animations, Swift animation durations, Compose `tween`), with the nearest elevation role or duration token as the fix. Test: `test_review_finds_shadows_and_durations`. |
| 10 | Q-comp-02 and `engine.py` | Q-comp-02 defaults to 25 core components; the engine's starting inventory has 22. | **Fixed** 2026-09-24 (F1): the engine starts with the 25 core components that Q-comp-02 lists. That number is backed by L08 (DC-L08-01: the components in 8-10 of the 10 benchmark systems). The old list of 22 mixed in extended parts (toast, skeleton, icon button). Test: `test_starting_inventory_matches_q_comp_02`. |
| 11 | `engine.py` validation report and `build` | The report says errors are "to fix before export", but `build` exports first and validates afterwards. | **Fixed** 2026-09-24 (F1): `build` now generates, validates, and stops before exporting when there are errors. DESIGN.md and the preview are not refreshed then either. `--force` exports anyway. The report wording ("to fix before you export") now matches. Test: `test_build_stops_before_export_on_errors`. |
| 12 | `synthesis/levers.json` and `engine.py` | `type.emphasizedVariants` turns on at Expression 67 and above, but the engine never emits emphasized text styles. | **Fixed** 2026-09-24 (F1): at Expression 67 and up the engine emits `text.emphasized.*`, each the same size as its baseline style and one weight heavier (next weight the system already has). Style-count lints skip them. Test: `test_emphasized_variants_from_expression_67`. |
| 13 | `engine.py` spacing `$description` | Lists multipliers up to 20; the engine emits `space.96` (multiplier 24). | **Fixed** 2026-09-24 (F1): the `$description` is built from the multiplier list, so it now ends at x24. |
| 14 | Spec section 7.4 and `engine.py` | Spec requires lowercase token path segments; the engine emits camelCase (`boldHover`, `onAccent`). | **Fixed** 2026-09-24 (F1), decided by the orchestrator (DECISIONS.md 01:32): every token path segment is lowercase kebab-case (`color.bg.accent.bold-hover`, `color.text.on-accent`, `radius.control-sm`, `font.line-height`). CSS, Swift and Compose names do not change. Examples, glossary token paths and data regenerated. Test: `test_path_segments_are_lowercase_kebab`. |
| 15 | `engine.py set` on a derived parameter or token path | Found by F1: `engine.py set type.ratio 1.333` stored a nested `overrides.type.ratio`, so the override never applied. | **Fixed** 2026-09-24 (F1): overrides are keyed by the whole path. Checked in `test_build_stops_before_export_on_errors`. |

The glossary describes what the engine does today, so it stays correct whichever way these are resolved. `python3 tools/check_glossary.py --refresh-tokens` re-reads the engine after any change, and `--build` flags glossary entries that no longer match.
