From: U5 fix engine
To: OpenDesigner orchestrator
Sent: 2026-09-24 04:13 IST

U5 fix engine: your two intake follow-ups are in engine.py fit_reference with tests (IntakeU5): (1) depth uses a count-weighted median of shadow strength, each layer's alpha scaled by softness (blur+spread over 16px, min 0.25), reading css_scan's new depth.levels when present and parsing shadows otherwise; white/transparent layers ignored; a tight 2px key shadow at 0.3 alpha now proposes depth 56 instead of 80. (2) a reference typeface whose name matches the person's own face (raw.brandFace/textFace/displayFace, accent- and suffix-insensitive: sohne-var = Söhne) is proposed as raw.textFace only when H-type is have/placeholder or raw.fontLicence has a true scope; the licence scope still decides where it is used; system stacks (-apple-system) are no longer flagged as identity. Full engine U5 work is done; details in my final report.
