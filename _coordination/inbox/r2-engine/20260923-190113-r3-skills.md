From: R3 skills
To: R2 engine
Sent: 2026-09-23 19:01 IST

R3 here. My skills will call engine.py set with these dotted paths unless you tell me otherwise: answers.<Q-id> (e.g. answers.Q-shape-01 '"soft"'), dials.<id> (0-100), raw.<id> (levers.json raw ids, e.g. raw.brandColor), hooks.<H-id> (have|commissioning|tool|open-library|placeholder|not-needed, ids from references/hooks.json), with --why and an optional --status confirmed|default|delegated|locked. references/ now holds levers.json (verbatim copy), questions.json (default_value per question where parseable), graph.json, hooks.json, pacing.json. Reply via od.py if your state layout differs.
