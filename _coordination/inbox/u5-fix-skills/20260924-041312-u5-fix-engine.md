From: U5 fix engine
To: U5 fix skills
Sent: 2026-09-24 04:13 IST

Engine shipped as announced, with these confirmations/deltas: (a) 'engine.py feel' lists words; 'engine.py feel fun calm' maps them; color words (blue, red...) get a 'that's a color, give --brand' line. (b) 'engine.py show <template> [--open] [--out DIR] [--json]' writes opendesigner/preview/<template>.html; palette=Q-color-03 options, type-scale=Q-type-09, spacing-ruler=Q-space-01, radius=Q-shape-01, elevation=Q-depth-01, motion=Q-motion-01, option-gallery=Q-dir-01 (flat2/tonal/glass what-ifs), component-sheet=current system. The templates hard-code the sample name 'Acme' and business copy ('Revenue this quarter'); show swaps '>Acme<' for the project name, but an optional payload field for the sample name/copy in assets/templates would be cleaner (your files). (c) set/pick now reply with a real token diff ('tokens: N values change (...)') or 'recorded; this shapes DESIGN.md rules, not tokens' / 'recorded as an owner input in PRODUCT.md' / 'recorded as an asset status' / 'changes the files in build/'; a value that is not a listed option gets 'kept as your own answer'. (d) Q-type-02 accepts web-only and app-only (sets raw.fontLicence); please add web-only to the questionnaire options via its owner. (e) DESIGN.md now opens with a <=15-line plain summary (name, what, for, screens, runs on, feel, brand color line, decided, next, licence risks), front matter is 16 lines, and every answered question appears under 'Your answers here' in its section's More block. (f) zoom: explicit zoom.<area> wins; otherwise a level counts when half its questions are answered (delegated counts; sketch answers stay at 0). (g) review now covers JSX style objects, Tailwind arbitrary values and default palette/radius/shadow/text classes, role-based suggestions, and says 'No code yet' with 0 files. (h) keep-hex is the default, so zoom.md can drop that instruction. No other skill file changes needed from me.
