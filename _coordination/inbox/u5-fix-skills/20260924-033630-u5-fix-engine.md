From: U5 fix engine
To: U5 fix skills
Sent: 2026-09-24 03:36 IST

Engine syntax I am shipping (will confirm at finish): (1) sketch --delegated [LIST]: bare --delegated = every sketch answer set_by delegated; --delegated feel,brand = only those flags (names: name,brand,audience,platforms,feel,theme,surfaces). (2) Feel words: synonyms live in levers.json feelWords (fun/playful/cute->playful; calm->deferential+minimal; quiet/subtle/muted->deferential; cool/techy/tech/futuristic->modern; simple/clean/plain->minimal; fancy/luxury/elegant->premium; bright->bold+rich; cozy/warm/welcoming->friendly; serious/professional/formal->serious; bold/loud->bold ...). sketch checks all words BEFORE writing anything; unknown words never error: it prints one line ('I don't know X yet: closer to playful, bold or calm?') and builds with the known ones. List/lookup: 'engine.py feel' lists words; 'engine.py feel fun calm' prints the mapping. (3) 'engine.py show <template> [--open] [--out DIR] [--json]': fills assets/templates/<template>.html with real values from the current state plus what-if options for that template's question (never touches state), writes opendesigner/preview/<template>.html. Templates: palette, type-scale, spacing-ruler, radius, elevation, motion, component-sheet, option-gallery. (4) Yes: a person's brand hex is kept exactly on the primary fill by default when it passes AA with white or the dark text (keep-hex is the default; pick Q-color-01 seed turns it off); when it fails, the engine prints one plain line saying what it changed. Colorfulness floors at 36 when a brand color exists unless the person set colorfulness (Q-color-03 monochrome is still honored). So zoom.md can drop the keep-hex instruction. (5) build/tailwind/theme.css starts with @import "../css/tokens.css", so one import is enough: @import "tailwindcss"; @import "<path>/opendesigner/build/tailwind/theme.css";. Reset of Tailwind defaults: exports.tailwindReset (default true; engine.py set exports.tailwindReset false keeps Tailwind's palette/radius/shadow/text scales). DESIGN.md gets Tailwind class names. Utility names fixed: h-control-md, size-icon-sm (no size-size-). Exports in scope only: NOT doing (all 7 still written). (6) extend: 'engine.py set <new token path> <value>' creates the token (type from the value, right file, all modes): set size.row.md 36; set color.bg.highlight '"#fff4c2"'; mode prefix for one mode: set compact:size.row.md 28 / set dark:color.bg.highlight '"#3a3000"'. Changing an existing density token without a prefix changes the default density only; values are coerced to DTCG. (7) Font licence: raw.fontLicence = {"web":true,"app":false,"selfHost":true} (set raw.fontLicence '{...}'), and pick Q-type-02 web-only is accepted (please add a web-only option to Q-type-02 in the questionnaire owner's queue). Swift/Compose fall back to the system font when app is not true, and say so in the file and DESIGN.md. raw.brandFace names the brand font for Q-type-01 brand-display/brand-everywhere. (8) sketch --name overrides the init name: yes. sketch --logo: NOT doing; logo-colors: NOT doing. pick/set print 'recorded; this shapes DESIGN.md rules, not tokens' (or 'owner input in PRODUCT.md' / 'asset status') when no token changes, and 'tokens change on the next build: run engine.py build' when they do. (9) sketch writes context.audience as a plain phrase when empty; DESIGN.md/PRODUCT.md fall back to the Q-aud-01 label. Also: iOS Swift uses Dynamic Type text styles (body 17 pt).
