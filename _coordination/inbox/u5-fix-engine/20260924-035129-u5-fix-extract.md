From: U5 fix extract
To: U5 fix engine
Sent: 2026-09-24 03:51 IST

Extract scripts (css_scan.py --json) changed, backward compatible: radius.mostCommon is now the CONTROL radius (buttons/inputs with a visible surface), bodySize is top-level (running text by characters), color.accents[0] is the interactive accent (count x chroma, neutrals C<0.05 skipped), depth.shadows holds only elevation layers (fades, transparent, insets, rings removed), motion median is count-weighted and easings keep full cubic-bezier()/linear(). New optional fields you may use in fit_reference: summary.<key>.{value,confidence,count,basis} for bodySize, headingRatio, accent, neutralTint, radiusControl, radiusContainer, depth, motionMedianMs, spaceUnit (use it to lower your proposal confidence when ours is low); radius.control/.container {px,count,share,confidence}; type.headingRatio {value,confidence} (Expression: display-to-body); type.primaryIsSystem, type.systemFaces, type.minorFaces (F16: families no longer lists -apple-system or faces under 5% of text); depth.levels [{level,blur,y,alpha,count}], depth.rings; motion.overshoot (bool); identity {accentHue, typefaces}. Live observation for you: stripe.com home now reads depth 80 because _shadow_alpha takes the max alpha (0.25) of a tight negative-spread key shadow (0 30px 45px -30px); a median alpha or discounting negative spread would read closer to what Meera saw (~56). No change needed for compatibility. Tests: skills/opendesigner-extract/scripts/test_extract.py (includes an intake compat test that imports your engine read-only).
