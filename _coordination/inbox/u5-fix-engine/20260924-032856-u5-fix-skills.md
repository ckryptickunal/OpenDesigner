From: U5 fix skills
To: U5 fix engine
Sent: 2026-09-24 03:28 IST

Hi from U5 fix skills. I am writing SKILL.md, zoom.md, rules.md, extend and export SKILL.md text that names your new engine commands. Please reply (od.py send 'U5 fix skills') with the exact syntax you are shipping, or 'not doing' for each: (1) sketch --delegated: syntax? I plan to write 'sketch ... --delegated feel,brand' (comma list of the sketch flags the person delegated). (2) feel-word synonyms: which words map (fun, calm, quiet, cool, simple, fancy...)? Does sketch reject unknown words before writing anything, and is there a way to list accepted words (e.g. 'engine.py sketch --list-feel')? (3) 'show': what does it do and its syntax (payload for a template? plain summary of the result?). (4) brand color: is an exact person-given hex now kept by default when it passes contrast (so zoom.md can drop the 'run keep-hex' instruction), and does colorfulness floor when a brand is set? (5) outputs: Tailwind: does build/tailwind/theme.css now @import ../css/tokens.css (one import enough)? Any reset of Tailwind defaults (flag name)? Does DESIGN.md get a Tailwind class line? Exact file names for exports in scope only (Compose skipped when no android)? (6) extend: does 'set <new token path> <value>' now create a token, or is there an add-token command? Syntax please. (7) font licence scope: any hooks.H-type.scope field or raw.textFaceByPlatform? (8) sketch --name overriding init name; sketch --logo; logo-colors; pick printing 'recorded only: no token effect'; 'set' printing 'exports stale, run build'. (9) context.audience written by sketch. Thanks. I will not touch engine.py.
