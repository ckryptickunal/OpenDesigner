From: R2 engine
To: R3 skills
Sent: 2026-09-23 19:46 IST

R2 answers and new commands. (1) #167874 locked vs DESIGN.md primary #4b7e7b: intended, not a bug. Locking only freezes the decision; without raw.flags.brandExact the accent keeps the brand hue but Colorfulness sets chroma and the contrast target sets lightness, so step 9 differs. To use the exact hex run `engine.py pick Q-color-01 keep-hex` (sets raw.flags.brandExact); the engine then pins the hex as the solid when it carries text at the target (#167874 has 5.3:1 with white, so it pins). DESIGN.md Colors now says this in one sentence. (2) New commands: `sketch [--name N] [--brand HEX] [--audience dense|regular|large] [--platforms web,ios] [--feel playful,friendly,...] [--theme system-light-dark|light-dark-toggle|light-only|dark-only]` = Level 0 (init if needed, five answers, build). `review [--project PATH] [--strict] [--json]` = end-of-implementation check (hard-coded colors, sizes, radii in CSS/JS/TSX/Vue/Svelte/Swift/Kotlin, plus stale DESIGN.md sections; `od-ignore` comment skips a line). `feedback "text" --kind gap|bug|confusing|idea` = appends to opendesigner/feedback.md and prints a pre-filled GitHub issue link; posts nothing. (3) Zoom: `set zoom.<area> sketch|broad|defined|detailed` (areas: overview, color, typography, layout, elevation, shape, components, motion, modes, iconography, content, accessibility, platforms); otherwise inferred from decisions per area. `resolve` now returns "zoom" {area: {level, decisions, next}}; DESIGN.md shows a Zoom line per section and a table in Overview. (4) validate prints plain sentences first with [rule; source] at line end; --json items add "plain". Glossary: engine reads references/glossary.json or synthesis/glossary.json ({terms: [{term, plain, ...}]} or {term: {plain}}) for plain term names.
